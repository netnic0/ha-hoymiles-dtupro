# Development dependencies for ha-hoymiles-dtupro.
# Install with: pip install -r requirements-dev.txt
# (or: pip install -e ".[dev]" — same set, sourced from pyproject.toml)

pymodbus[serial]>=3.6,<4.0
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.6
mypy>=1.10
pre-commit>=3.7
